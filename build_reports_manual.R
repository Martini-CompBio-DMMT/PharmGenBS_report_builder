library(PharmGenBS)

PharmGenBS::create_reports(file_info = "samples_info.tsv", file_genos = "samples_genos.tsv")
PharmGenBS::create_internal_reports(file_info = "samples_info.tsv", file_genos = "samples_genos.tsv")
